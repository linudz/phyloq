# Nextflow state, logs, locks and Conda-environment cache
.nextflow*

# Nextflow task staging and published outputs
work/
results/
results.smoke*/
work.depth_profiles/
depth_profile_results*/

# Slurm driver logs (keep the directory itself in Git)
logs/*
!logs/.gitkeep
